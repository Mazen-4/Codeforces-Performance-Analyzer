[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_20", "nodePackages.npm"]

[phases.install]
cmds = ["bash scripts/setup_render.sh"]

[start]
cmd = "cd website/server && node server.js"

[variables]
NIXPACKS_NODE_VERSION = "20"
